file( GLOB SOURCES "*.*" )
add_executable( "Utils.Algorithms" ${SOURCES} )
source_group( TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SOURCES} )
set_property( TARGET "Utils.Algorithms" PROPERTY FOLDER "Utils" )
target_link_libraries( "Utils.Algorithms" PUBLIC "STL" )
