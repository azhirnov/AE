
game objects
	player
	npc
	vechicle
	building
	environment
	other

visibility objects
	2d tile
		- landscape
		- building (outer part only)
		- environment (trees, ...)
		- npc, vechicles, ...
	building (inner part)
		- portals between levels

render objects
	model ???



main loop:
	- update physics & AI
		...
	- test visibility:
		for each viewport (primary cameras):
			test intersection with secondary cameras (shadow/reflection/...)
			build camera graph
		for each camera in camera graph:
			test tile (quad tree) visibility
			test objects in tile visibility